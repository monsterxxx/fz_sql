SELECT 'Всего занималось' AS COLUMN, 'Оплатили' AS COLUMN1, CRM_PAYMENT_PRE.*
FROM CRM_PAYMENT_PRE LEFT JOIN CRM_DEBT ON (CRM_PAYMENT_PRE.PERSON_ID = CRM_DEBT.PERSON_ID) AND (CRM_PAYMENT_PRE.PAYMONTH = CRM_DEBT.DEBTMONTH)
WHERE CRM_DEBT.PERSON_ID IS NULL
