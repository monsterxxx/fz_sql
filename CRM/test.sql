SELECT * FROM (((CRM_ATTENDANCE LEFT JOIN CRM_PAYMENT ON CRM_PAYMENT.PERSON_ID = CRM_ATTENDANCE.PERSON_ID) LEFT JOIN CRM_DEBT ON CRM_DEBT.PERSON_ID = CRM_ATTENDANCE.PERSON_ID) LEFT JOIN CRM_TRY ON CRM_TRY.PERSON_ID = CRM_ATTENDANCE.PERSON_ID) LEFT JOIN CRM_FREE ON CRM_FREE.PERSON_ID = CRM_ATTENDANCE.PERSON_ID
WHERE CRM_DEBT.PERSON_ID IS NULL AND CRM_TRY.PERSON_ID IS NULL AND CRM_PAYMENT.PERSON_ID IS NULL AND CRM_FREE.PERSON_ID IS NULL
