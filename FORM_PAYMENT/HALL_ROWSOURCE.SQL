SELECT FULLNAME, T1.HALL_ID
FROM (
  SELECT DISTINCT HALL_ID
  FROM GROUPS
  WHERE ORGANIZATION_ID = 76 AND HIDDEN = FALSE AND POTENTIAL = FALSE
) AS T1
INNER JOIN HALL ON (HALL.HALL_ID = T1.HALL_ID)
