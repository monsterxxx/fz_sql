SELECT COMPANY,
  PERSON_ID,
  DATESERIAL(YEAR(PAYMENT_DATE), MONTH(PAYMENT_DATE), 1) AS BUDMONTH,
  SUM(PAYMENT.SUMMA) AS SUMMA
FROM PAYMENT
WHERE PAYMENT_TYPE_ID = 6
GROUP BY COMPANY, PERSON_ID, YEAR(PAYMENT_DATE), MONTH(PAYMENT_DATE)
