  SELECT 'Размер' AS COLUMN,
    CALENDAR_MONTH AS Month,
    GROUP_ID,
    GROUP_SIZE AS AMOUNT
  FROM GE_COMBINED
UNION ALL
  SELECT 'Заполненность',
    CALENDAR_MONTH,
    GROUP_ID,
    FULLNESS
  FROM GE_COMBINED
UNION ALL
  SELECT 'Выручка',
    CALENDAR_MONTH,
    GROUP_ID,
    INCOME
  FROM GE_COMBINED
UNION ALL
  SELECT 'План прибыли',
    CALENDAR_MONTH,
    GROUP_ID,
    PROFIT_PLAN
  FROM GE_COMBINED
